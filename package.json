{
  "name": "nxt-motors-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
